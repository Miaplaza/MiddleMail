image: mcr.microsoft.com/dotnet/core/sdk:3.1


stages:
  - build
  - test

build:
  stage: build
  script:
      - "dotnet build"

test:
  stage: test
  - name: rabbitmq-management:3.8-alpine
    alias: rabbitmq
    
  script: 
      - "dotnet test"
